# Advanced Wireline Data Preprocessing System - Requirements
# Install with: pip install -r requirements.txt

# Core Dependencies (Required)
numpy>=1.20.0
pandas>=1.3.0
matplotlib>=3.3.0

# LAS File Support (Recommended)
lasio>=0.29

# Advanced Features (Optional but Recommended)
# Uncomment to enable advanced gap filling and denoising:
scipy>=1.7.0
scikit-learn>=1.0.0
pywavelets>=1.1.0

# System Monitoring (Optional)
psutil>=5.8.0

# Build Tool (For creating standalone executable)
# Uncomment if you want to create a standalone .exe/app:
# pyinstaller>=5.0.0

# Note: tkinter is included with most Python installations
# If missing, install via your system package manager:
#   Windows: Usually included with Python
#   Linux: sudo apt-get install python3-tk
#   macOS: Usually included with Python

