# Runtime dependencies for POLISH (also used by CI).
# Tkinter ships with the interpreter; on Linux install python3-tk.

numpy>=1.20.0
pandas>=1.3.0
matplotlib>=3.3.0
lasio>=0.29
scipy>=1.7.0
scikit-learn>=1.0.0
pywavelets>=1.1.0
psutil>=5.8.0
